/*public class Check_Thread extends Thread{

    public void run() {
        while (running)
        if thread state =/ active => deleteinactive user
    }
}*/

/* A mettre dans la connection tcp (coté serveur qui attend la connection) :
au niveau du while qui boucle en attente de message
cf https://www.alpharithms.com/detecting-client-disconnections-java-sockets-091416/
// Check for client disconnection
                if(inputStream.read() == -1){
                    // Notify via terminal, close connection
                    System.out.println("client disconnected. Socket closing...");
                    socket.close();
                }
 */
